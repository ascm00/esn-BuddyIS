# # Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# # More GitHub Actions for Azure: https://github.com/Azure/actions

# name: Build and deploy Node.js app to Azure Web App - Buddyistest

# on:
#   push:
#     branches:
#       - deploy/prod
#   workflow_dispatch:

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Node.js version
#         uses: actions/setup-node@v3
#         with:
#           node-version: '20.x'

#       - name: npm install, build, and test
#         run: |
#           npm install
#           npm run build --if-present
#           npm run test --if-present

#       - name: Zip artifact for deployment
#         run: zip release.zip ./* -r

#       - name: Upload artifact for deployment job
#         uses: actions/upload-artifact@v4
#         with:
#           name: node-app
#           path: release.zip

#   deploy:
#     runs-on: ubuntu-latest
#     needs: build
#     environment:
#       name: 'Production'
#       url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
#     permissions:
#       id-token: write #This is required for requesting the JWT

#     steps:
#       - name: Download artifact from build job
#         uses: actions/download-artifact@v4
#         with:
#           name: node-app

#       - name: Unzip artifact for deployment
#         run: unzip release.zip
      
#       - name: Login to Azure
#         uses: azure/login@v2
#         with:
#           client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_56EF959E17D54687965A76F3CA572ADF }}
#           tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_8521818BEEF94FAD9097585AD0CD8C34 }}
#           subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_B4CB397F3892495B99A4FF50105510B8 }}

#       - name: 'Deploy to Azure Web App'
#         id: deploy-to-webapp
#         uses: azure/webapps-deploy@v3
#         with:
#           app-name: 'Buddyistest'
#           slot-name: 'Production'
#           package: .
          

name: Deploy

on:
  push:
    branches:
      - 'deploy/prod'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: yarn install --immutable

      - name: Build and deploy Contember to production
        run: yarn run deploy ${{ secrets.CONTEMBER_DEPLOY_DSN }} --yes

#     Uncomment this block if you are using Cloudflare Workers for Contember actions

#      - name: Setup Contember actions variables
#        run: bash .github/scripts/setup-actions-variables.sh
#        env:
#          CONTEMBER_DEPLOY_DSN: ${{ secrets.CONTEMBER_DEPLOY_DSN }}
#          CONTEMBER_ACTIONS_VARIABLES: ${{ secrets.CONTEMBER_ACTIONS_VARIABLES }} # [{ name: "apiKey", value: "new-api-key-value" }, { name: "baseUrl", value: "http://localhost/foo-bar" }]
#
#      - name: Deploy action worker
#        uses: cloudflare/wrangler-action@v3.4.0
#        with:
#          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
#          workingDirectory: "worker"
#          secrets: |
#            CONTEMBER_API_TOKEN
#            CONTEMBER_ACTIONS_SECRET_KEY
#        env:
#          CONTEMBER_API_TOKEN: ${{ secrets.CONTEMBER_API_TOKEN }}
#          CONTEMBER_ACTIONS_SECRET_KEY: ${{ secrets.CONTEMBER_ACTIONS_SECRET_KEY }}
